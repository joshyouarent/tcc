# Copy to .env.local for local development. Both values are also set in Vercel.
# These are the browser-safe publishable credentials, not the secret key.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx
